# 這是 LabCamp 的環境變數範例檔案。你可以將此檔案複製為 .env，並依照需求修改其中的設定值。
POSTGRES_PASSWORD="replace-with-a-random-database-password"
DATABASE_URL="postgresql://labcamp:replace-with-a-random-database-password@localhost:5432/labcamp"
AUTH_SECRET="replace-with-a-random-secret"
NEXTAUTH_URL="http://your-domain-or-ip"
AUTH_URL="http://your-domain-or-ip"
LETSENCRYPT_EMAIL="admin@example.com"

# 這是執行 seed 腳本時，將建立之管理員使用者的密碼。建議你在執行 seed 腳本之前，先將此密碼修改為安全性較高的密碼。
SEED_ADMIN_PASSWORD="replace-before-running-the-seed"
